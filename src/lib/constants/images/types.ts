export interface ImageAsset {
  src: string;
  alt: string;
  thumbnail?: string;
}

export interface ProductImage {
  main: string;
  alt: string;
  thumbnail: string;
}

export interface GalleryImage {
  src: string;
  alt: string;
  thumbnail: string;
}